import Tag from '@site/src/components/@mdx/_Tag.mdx'


### #TS-1 / Type Definition: <Tag>PascalCase</Tag>

```tsx
interface Apple {...}
type TreeBug = {...}
```




### #TS-2 / 전역적으로 사용하는 Utility(Generic) 타입은 예시코드를 사용하기

전역적으로 사용하는 utility 타입의 경우엔 타인이 사용할 가능성이 높기 때문에, 미리 결과를 알 수 있는 예시코드를 포함해 줍니다.

```tsx
// type Example = ItemOf<['a', 'b', 'c']>;
// Example = "a" | "b" | "c"

export type ItemOf<T extends Array<any> | readonly any[]> = T[number];
```
